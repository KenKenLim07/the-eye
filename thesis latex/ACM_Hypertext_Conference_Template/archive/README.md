# Archive

This folder contains the earlier auto-converted thesis outputs (Pandoc conversion, extracted images, and generated BibTeX) that produced many compilation errors during drafting.

The active draft to compile on Overleaf is now `main.tex` at the project root, which is a clean ACM `acmart` compressed outline with manual references (no BibTeX).

